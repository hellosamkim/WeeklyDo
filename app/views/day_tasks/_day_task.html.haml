%p= day_task.task
= link_to "Mark as Finished", finished_day_day_task_path(@day, day_task.id), method: :patch 
= link_to "Delete", day_day_task_path(@day, day_task.id), method: :delete, data: { confirm: "Are you sure?" }